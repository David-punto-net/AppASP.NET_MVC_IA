@model AppVentasWeb.Data.Entidades.Categoria

@{
    ViewData["Title"] = "Details";
}

<div class="card">
    <div class="card-header">
        <h5 class="card-title">Detalle Categoría</h5>
    </div>

    <div class="card-body">
        <div class="row">
            <div class="col-md-8">

                @*<-- Campos del formulario en visat parcial -->*@
                <partial name="_CategoriaDetalle" />

                <div class="form-group row">
                    <div class="col-sm-1"></div>
                    <div class="col-sm-4">
                        @* <a asp-action="Edit" class="btn btn-success" asp-route-id="@Model?.Id">Editar</a> *@
                        @* <a asp-action="Index" class="btn btn-primary">Volver</a> *@
                        <div>
                            <a asp-action="Index" class="btn btn-outline-success"><i class="fa-solid fa-arrow-rotate-left"></i> Regresar</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
